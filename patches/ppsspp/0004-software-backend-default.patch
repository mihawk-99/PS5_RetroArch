diff --git a/libretro/libretro_core_options.h b/libretro/libretro_core_options.h
index 333ac3a..cbf2a5c 100644
--- a/libretro/libretro_core_options.h
+++ b/libretro/libretro_core_options.h
@@ -301,7 +301,17 @@ struct retro_core_option_v2_definition option_defs_us[] = {
          { "none",   "None" },
          { NULL, NULL },
       },
+#if PPSSPP_PLATFORM(PS5)
+      // "none" selects the libretro software context, which is PPSSPP's own CPU
+      // rasterizer handing XRGB8888 frames to the frontend. It is the default on
+      // this platform because the Vulkan renderer is not usable yet: the console's
+      // Vulkan driver advertises no combined depth/stencil format, and PPSSPP
+      // asserts on that during device creation. The Vulkan entry above stays
+      // selectable for when the driver grows it.
+      "none"
+#else
       "auto"
+#endif
    },
    {
       "ppsspp_software_rendering",
